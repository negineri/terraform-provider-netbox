---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "netbox_vlan_group Resource - netbox"
subcategory: ""
description: |-
  Manages a VLAN Group in Netbox.
---

# netbox_vlan_group (Resource)

Manages a VLAN Group in Netbox.

## Example Usage

```terraform
resource "netbox_vlan_group" "example" {
  name        = "Example VLAN Group"
  slug        = "example-vlan-group"
  description = "Created via terraform-provider-netbox"
  min_vid     = 1
  max_vid     = 4094
  scope_type  = "dcim.site"
  scope_id    = 1
}

resource "netbox_vlan" "example" {
  name        = "Example VLAN"
  vid         = 100
  status      = "active"
  description = "Created via terraform-provider-netbox"
  group_id    = netbox_vlan_group.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the VLAN group.

### Optional

- `description` (String) Description for the VLAN group.
- `max_vid` (Number) Maximum VLAN ID (1-4094).
- `min_vid` (Number) Minimum VLAN ID (1-4094).
- `scope_id` (Number) The ID of the scope object.
- `scope_type` (String) The type of the scope object (e.g., dcim.site, dcim.location, dcim.region, dcim.sitegroup, virtualization.cluster, virtualization.clustergroup).
- `slug` (String) URL-friendly unique shorthand for the VLAN group. If omitted, auto-generated from name.

### Read-Only

- `id` (Number) The numeric ID of the VLAN group.
